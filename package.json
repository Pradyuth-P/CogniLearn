{
    "name": "cognilearn",
    "version": "1.0.0",
    "description": "CogniLearn MERN — root runner",
    "scripts": {
        "start": "concurrently --names \"SERVER,CLIENT\" --prefix-colors \"bgBlue.bold,bgGreen.bold\" \"npm start --prefix server\" \"npm run dev --prefix client\""
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}